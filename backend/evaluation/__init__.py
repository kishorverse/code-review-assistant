"""Evaluation harness: labeled datasets, ablation runs, scoring and benchmarks.

Run it from ``backend/`` with ``uv run python -m evaluation --help``. Datasets and
results live in the repository's top-level ``eval/`` directory.
"""
